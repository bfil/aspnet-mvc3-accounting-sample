@model IEnumerable<Accounting.Areas.Accounting.Models.Income>

@{
    View.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>

    <table>
        <tr>
            <th></th>
            <th>
                Description
            </th>
            <th>
                Date
            </th>
            <th>
                Amount
            </th>
            <th>
                Income Type
            </th>
        </tr>

    @foreach (var item in Model) {
    
        <tr>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IncomeId }) |
                @Html.ActionLink("Details", "Details", new { id=item.IncomeId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IncomeId })
            </td>
            <td>
                @item.Description
            </td>
            <td>
                @String.Format("{0:d}", item.Date)
            </td>
            <td>
                @String.Format("{0:F}", item.Amount)
            </td>
            <td>
                @item.IncomeType.Description
            </td>
        </tr>
    
    }

    </table>


